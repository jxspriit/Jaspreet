export const environment = {
  production: false,
  // Used only if/when GithubStatsComponent is wired to the live GitHub REST API
  // (e.g. https://api.github.com/users/{githubUsername}). No secret keys required
  // for public GitHub read endpoints, so nothing sensitive lives here.
  githubUsername: 'jxspriit',
};
